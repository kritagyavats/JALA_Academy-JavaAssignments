//Ques 3) Finding the length of the string.

package A06_Strings;

public class Ques03 {
    public static void main(String[] args) {
        String str = "JALA Academy";

        int length =  str.length();     //Finds the length of the string

        System.out.println("String Length: " + length);
    }
}
